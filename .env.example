# Google Gemini API Key
# Get yours at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_key_here

# Optional: Mount a specific host directory as the default 'external_data'
# If you use docker-compose.override.yml, this is less important, but still good to have.
HOST_DATA_PATH=./data
