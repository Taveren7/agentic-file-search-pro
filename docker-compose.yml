version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ${HOST_DATA_PATH:-./data}:/app/external_data
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
